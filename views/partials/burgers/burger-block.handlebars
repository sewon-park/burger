<tr class="table-row">
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
			
    <td><button class="devourbtn" data-id="{{id}}" data-newdevoured="{{devour}}">
			{{!-- {{#unless devoured}}  --}}
			Devour it! </button>
		</td> 
</tr>